# Copy to .env and fill in. .env is gitignored.

OPENAI_API_KEY=

# Weave / W&B tracing
WANDB_API_KEY=
WEAVE_PROJECT=voice-self-correct-loop

# Google Cloud TTS + Speech-to-Text (auth via `gcloud auth application-default login`, no key needed here -- both TTS and ASR use the same ADC credentials)
GOOGLE_CLOUD_PROJECT=project-b7a6bd06-d101-4f46-a34
TTS_VOICE_NAME=en-US-Neural2-C
TTS_LANGUAGE_CODE=en-US

# Comma-separated trap words that gate the self-correction retry loop
TRAP_WORDS=coreweave,typesafe,marimo,aria,kirkcudbright,eyjafjallajokull,rybelsus,vraylar
TYPESAFE_API_KEY=your-typesafe-api-key
